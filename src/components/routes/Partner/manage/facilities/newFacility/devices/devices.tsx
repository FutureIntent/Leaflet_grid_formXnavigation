import styled from "styled-components";

const DevicesContainer = styled.div`

`;

const Devices = () => {
    const zzz="zzz";

    return(
        <DevicesContainer>
            DEVICES
        </DevicesContainer>
    );
}

export default Devices;