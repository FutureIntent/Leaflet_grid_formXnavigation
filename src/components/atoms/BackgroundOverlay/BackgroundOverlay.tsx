import styled from 'styled-components';

const BackgroundOverlay = styled.div`
    background: transparent linear-gradient(180deg, #070f39b3 0%, #070f3933 100%);
    height: 100%;
    position: relative;
    width: 100%;
    z-index: 1;
`;

export default BackgroundOverlay;
